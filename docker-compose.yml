services:
  tashan-assistant:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${APP_BIND_IP:-127.0.0.1}:${APP_PORT:-8000}:8000"
    volumes:
      - ./data:/app/data
